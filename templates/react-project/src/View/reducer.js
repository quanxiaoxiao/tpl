export const initialState = {
  navList: [
    {
      name: 'project',
      path: '/project',
    },
  ],
};

export const actions = {
};

export const reducer = (state, { type, payload }) => {
  switch (type) {
    default:
      throw new Error();
  }
};
